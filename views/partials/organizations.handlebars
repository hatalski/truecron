<script type="text/x-handlebars" data-template-name="organizations">
    <div>
        {{outlet}}
    </div>

    {{#link-to "organizations.add"}}Add organization{{/link-to}}
    {{#each model}}
        <li>{{#link-to "organizations.item" this}}{{name}}{{/link-to}}</li>
    {{/each}}
</script>
