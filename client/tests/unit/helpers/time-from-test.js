import {
  timeFrom
} from 'true-cron/helpers/time-from';

module('TimeFromHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeFrom(42);
  ok(result);
});
