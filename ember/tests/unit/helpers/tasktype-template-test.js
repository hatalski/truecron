import {
  tasktypeTemplate
} from 'true-cron/helpers/tasktype-template';

module('TasktypeTemplateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = tasktypeTemplate(42);
  ok(result);
});
