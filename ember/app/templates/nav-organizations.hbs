<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{name}}-Dev <span class="caret"></span></a>
    <ul class="dropdown-menu" role="menu">
    	{{#each}}
    		<li class="dropdown-header">{{name}}</li>
    		{{#each workspaces}}
    		<li>{{#link-to "dashboard"}}{{name}}{{/link-to}}</li>
    		{{/each}}
    	{{/each}}
    </ul>
</li>