<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{name}}-Dev <span class="caret"></span></a>
    <ul class="dropdown-menu" role="menu">
    	{{#each organization in organizations}}
    		<li class="dropdown-header">{{organization.name}}</li>
    		{{#each workspace in organization.workspaces}}
    		<li>{{#link-to "dashboard.organization.workspace" organization workspace}}{{workspace.name}}{{/link-to}}</li>
    		{{/each}}
    	{{/each}}
    </ul>
</li>